# Read by upstream's https://pkgs.netbird.io/install.sh.
#
# Anything other than "bin" makes its --update path bail out with
# "please use your system's package manager", which stops it from
# overwriting the Portage-managed binaries in /usr/bin.
package_manager=portage
